for notifications type :
-"added to group": when a member o creator add directly a user to be member into the group;
-"new follower": when a user get a new follower;
-"follow request": when a user sent a follow request to a private account;
-"group join request": when a user sent a join request to a group;
 